import * as React from 'react'

export const Label = props => (
  <label
    display='block'
    color='blue.8'
    fontSize={0}
    fontFamily='sans-serif'
    marginBottom={1}
    {...props}
  />
)
